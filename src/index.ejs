<%
var options = htmlWebpackPlugin.options;
%>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="viewport" content="minimal-ui, width=device-width, user-scalable=no, maximum-scale=1, minimum-scale=1" />

  <title>
    m-days
  </title>

  <style>
      html,
      body,
      #root {
          height: 100%;
          width: 100%;
          background: #000;
      }
  </style>
</head>

<body>
<noscript>
  You need to enable JavaScript to run this app.
</noscript>

<div id="root">
  <%= require('components/Bg/Bg.ejs')() %>
</div>

<script>
  function hideAddressBar() {
    window.scroll(0, 1);
  }

  function requestFullScreen() {
    const el = document.body;

    // Supports most browsers and their versions.
    const requestMethod = el.requestFullScreen
      || el.webkitRequestFullScreen
      || el.mozRequestFullScreen
      || el.msRequestFullScreen;

    if (requestMethod) {
      // Native full screen.
      requestMethod.call(el);
    }
  }

  setTimeout(() => {
    hideAddressBar();
  }, 100);

  window.addEventListener('orientationchange', hideAddressBar);

  document.addEventListener('click', requestFullScreen);
</script>
</body>
</html>
